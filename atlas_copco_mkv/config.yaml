name: Atlas Copco MKV Parser
version: "0.0.0.1"
slug: atlas_copco_mkv
description: "Parses Atlas Copco MKV files (example add-on)."
arch:
  - amd64
startup: application
init: false
boot: auto
options:
  input_path: /data/input.mkv
schema:
  input_path: str?
# NOTE: No 'image:' key to ensure Supervisor builds from this Dockerfile.
